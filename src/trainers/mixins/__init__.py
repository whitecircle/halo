"""Trainer spine (:mod:`~src.trainers.mixins.base`) and the sub-mixins it composes, plus the
opt-in mixins (``stored_metrics``) and function modules (``grad_clip``, ``loss_masks``,
``pp_gates``) that trainers use directly."""
