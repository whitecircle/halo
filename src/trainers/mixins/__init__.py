"""The trainer spine (:mod:`~src.trainers.mixins.base`), the sub-mixins it composes, the mixins a
trainer adds for itself (``stored_metrics``), and the function modules trainers call directly
(``grad_clip``, ``loss_masks``, ``pp_gates``)."""
