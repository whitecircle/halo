"""Script-side argument dataclasses: the per-method YAML/CLI surface on top of ``CommonScriptArguments``, the
parallelism flags, the field bundles shared with :mod:`src.configs` (``mixins``) and the range validator both
packages subclass (``validation``)."""
