"""Dataclasses a trainer is constructed with: the ``TrainingArguments`` subclasses of the toolkit's own
methods (SMPO, offline GRPO, distillation, classification, embedding) and the plain configs the trainers hold
(async rollout, rollout, environment). Script-side YAML/CLI surfaces live in :mod:`src.args`."""
