# Qwen3.5 122B-A10B — EP SFT example. 122B total / 9.77B active · 256 routed experts (top_k 8).
# CP unsupported (hybrid linear-attention layers). See agent-docs/models/qwen3_5.md.
# Single node (Blackwell): torchrun --nproc_per_node=8 scripts/training/sft.py <this config>
# Cross-node (2 nodes x 8 GPUs): add --expert_parallel_size=16 --ep_scope=global --max_length=8192
# (see skypilot/). max_length drops because cross-node dispatch rides the EFA proxy GIN, validated
# to 8192 tokens/rank per MoE forward; the 16384 below is a single-node NVLink shape.

# --- Model ---
model_name_or_path: Qwen/Qwen3.5-122B-A10B

# --- Dataset ---
dataset:
- HuggingFaceH4/ultrachat_200k@train_sft
conversation_field: messages
test_size: 0.01

# --- Parallelism & MoE precision ---
expert_parallel_size: 8               # 256 experts → 32/rank single-node. Cross-node (Hopper): EP=16 global.
moe_balancing: bias_update_transient  # aux_loss raises here (Qwen3_5MoeForConditionalGeneration reads
                                      # output_router_logits from kwargs, never from the config, so the
                                      # flag records router logits while router_aux_loss_coef never
                                      # reaches the loss) and the architecture has no exportable bias
                                      # slot, so exported checkpoints serve without the trained bias
                                      # (near-tie top-k flips vs training)
save_sharded_ep: false
fp32_router: true                     # cheap; stabilizes routing
fp32_grad_reduce: true                # fp32 grad reduction for many-rank / cross-node EP
# Experts stay bf16 (AdamWBF16, 6 B/param) — fp32 masters plus moments cost 16 B/param and OOM at 122B.

# --- Attention & kernels ---
# FA2 is pinned for packing. Left unset, the resolver auto-detects FA4 on Blackwell and then falls
# back to SDPA for this family — the FA4 backward NaNs at head_dim 256 with partial rotary.
attn_implementation: flash_attention_2
packing: true                         # fixed-length only — never padding_free (M-RoPE varlen crash)
max_length: 16384                     # CP unavailable; single-rank activation ceiling — drop to 8192 if OOM

# --- Training ---
per_device_train_batch_size: 1
per_device_eval_batch_size: 1
gradient_accumulation_steps: 8
num_train_epochs: 1.0
gradient_checkpointing: true

# --- Optimizer & LR schedule ---
optim: adamw_torch_fused
learning_rate: 4.0e-06
lr_scheduler_type: cosine
warmup_steps: 50
max_grad_norm: 1.0

# --- Checkpointing & evaluation ---
save_strategy: steps
save_steps: 500
eval_strategy: steps
eval_steps: 300
save_total_limit: 1
save_only_model: true
output_dir: checkpoints/qwen3.5-122b-a10b-ep

# --- Logging ---
logging_steps: 1
logging_first_step: true
report_to: wandb
remove_unused_columns: false
dataloader_num_workers: 2

# --- PEFT ---
use_peft: false

# --- Tokens & chat template ---
assistant_message_template: "<|im_start|>assistant\n"
pad_token: <|endoftext|>
eos_token: <|im_end|>
chat_template: jinja-templates/qwen3/qwen3-multiturn.jinja  # ultrachat is multi-turn; the -instruct template rejects >1 user turn
force_chat_template: true
